const name = "anant"
const repoCount = 50
// string interpolation -- fstring in python 
console.log(`Hey there, I'm ${name} and my repoCount is ${repoCount}`);

const gameName = new String('anant')
console.log(gameName[0]); //a
console.log(gameName.length); // 5
console.log(gameName.toUpperCase()); //ANANT

const newString = "     asdasda    "
console.log(newString); 
console.log(newString.trim()); //asdasda

// .replace(" ", " ")

// .includes(' ')

